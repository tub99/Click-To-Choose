import { QuestionMarkerDirective } from './question-marker.directive';

describe('QuestionMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new QuestionMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
